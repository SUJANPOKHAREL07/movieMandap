import MovieGalleryClient from '../components/MovieGalleryClient';

export default function Index() {
  return (
    <div className="min-h-screen bg-gray-50">
      <MovieGalleryClient />
    </div>
  );
}
